    # from django.db import models
    # from admin_auth.models import * 
    # from user_auth.models import * 

    # # Create your models here.


    # class OrderDetails(models.Model):

    #     shipping_address=models.ForeignKey(UserAdress,on_delete=models.CASCADE)
    #     user_details=models.ForeignKey(CustomUser,on_delete=models.CASCADE)
    #     orderdate=models.DateField(default=timezone.now)
    #     ordersatus=models.BooleanField